server:
  port: 8080
spring:
  application:
    name: user
  datasource:
    url: jdbc:postgresql://localhost:5432/task-tide
    username: postgres
    password: 1234567
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
task-tide:
  security:
    secret-key: ${SECRET-KEY:developer}
